# docker

Faz ilerledikçe eklenecek servis-özel Dockerfile'lar ve compose override
dosyaları için ayrılmış klasör (frontend, backend, ollama, vb.). Faz 1'in
tek servisi olan `mock-enterprise`'ın Dockerfile'ı kendi klasöründe
(`mock-enterprise/Dockerfile`) yer alıyor.
