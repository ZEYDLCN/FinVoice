[pytest]
pythonpath = ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    slow: requires a running Ollama server with a pulled model (excluded by default)
addopts = -m "not slow"
