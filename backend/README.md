# backend (Faz 4+)

LangGraph tabanlı Voice Agent Orchestrator ve tool-calling katmanı burada
yaşayacak: `agents/`, `tools/`, `services/`, `models/`, `schemas/`, `api/`.

`mock-enterprise/` servisindeki API'leri sarmalayan tool fonksiyonları
(`get_policy`, `create_claim`, `freeze_card`, ...) bu klasörde tanımlanacak.

Detaylar için bkz. kök `ROADMAP.md` → Faz 4.
